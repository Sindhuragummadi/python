pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                checkout scm

                sh 'jenkinsfile1'
                sh 'jenkinsfile1'
                sh '''#!/bin/sh'''


                

            }
        }
    }
}
